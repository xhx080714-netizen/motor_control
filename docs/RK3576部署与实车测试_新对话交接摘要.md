# RK3576 小车部署与实车测试：新对话交接摘要

更新时间：2026-08-17
工程目录：`motor_control` 仓库根目录  
适用范围：小车、RK3576、前后两块双电机驱动板  
寄存器唯一依据：公司《第四版.doc》前部码表

## 0. 新对话先执行的规则

1. 先完整读取本文，再读取第 1 节列出的文件。
2. 只处理本机工程时，不连接 RK3576、不访问 tty、不发送任何真机帧。
3. 真机操作必须由用户明确提出，并由用户在现场确认车辆架空和硬件停机手段。
4. 未完成现场视觉确认前，不冻结四轮物理轮位或车身正反方向；寄存器顺序按
   公司表固定为 `0x0000=M1`、`0x0001=M2`。
5. 不从其他车辆资料、旧测试记录、通用 Modbus 习惯或《第四版.doc》后部错误
   示例推导小车寄存器含义。
6. 本文已包含继续工作的必要状态，不依赖此前对话内容。

## 1. 必须先读的文件

1. 公司《第四版.doc》前部寄存器表（外部受控文件）
2. `docs/小车控制命令操作手册.md`
3. `docs/小车follow_iou双板双电机实车测试记录_2026-08-15.md`
4. `src/sand_rake_hw_tools/README.md`

公司原始文件由用户另行保存，不随本仓库发布。寄存器、状态位和控制字发生
冲突时，只以该文件前部码表为准；后部示例已由用户明确判定为错误。历史参考
源码只可用于解释现有软件收发行为，不能覆盖公司码表。

## 2. 本地工程最终审查结果

2026-08-17 用户将职责范围明确为“小车电机驱动和底盘控制”，本地工程据此
完成再次清理：

```text
ROS 2 包：3
保留：sand_rake_interfaces、sand_rake_control、sand_rake_hw_tools
build/install/log：无
```

已清除的内容包括：

- 工作区生成产物和编辑器目录；
- 其他车型配置和过期、重复文档；
- 未完成实车验证的板级映射配置、消息、节点、launch 和测试；
- 三个只有占位作用的无效测试；
- 孤立 RViz 配置及空目录；
- CMake 中重复复制核心源码的 fallback；
- 未使用的通信统计代码和要求标准 `0x06` 严格回显的客户端接口。
- 从 `main` 同步但与当前职责无关的感知、视觉、通用工具、相机/雷达 bringup
  包，以及录包脚本和相应团队协作文档。

`sand_rake_interfaces` 不能删除，因为底盘安全控制和集成测试直接依赖其中的
`SafetyEvent`。底盘 Mock 启动入口由 `sand_rake_control` 自身提供，无需保留
重复 bringup 包。以上均为本地工作树清理，没有修改远端 GitHub `main`，也没有
提交或推送。工程于 2026-08-16 重命名为 `motor_control`。

## 3. 当前硬件事实

| 目标 | 接口 | RK3576 设备 | 串口参数 | 从站地址 |
|---|---|---|---|---|
| 前轮驱动板 | JP17 | `/dev/ttyS6` | 115200 8N1 | `0x01` |
| 后轮驱动板 | JP18 | `/dev/ttyS1` | 115200 8N1 | `0x01` |

- 实车测试时车辆已架空。
- 面板常亮绿灯，用户现场确认处于 FOLLOW 模式。
- 两块板读取 `0x0008` 均得到 `0x0001`，与面板状态一致。
- 每块板只有 M1、M2 两个电机控制槽位。
- 公司表只定义板内 M1/M2，没有定义它们对应车身左右轮。
- 最后一次现场安全状态确认是双板双电机 Coast，四轮可用手自由转动。
- 2026-08-17 已重新连接 RK3576、更新车机源码并完成下述受控诊断；最后一次
  真机命令以双电机 Coast 结束。

四轮关系尚未冻结：

| 板内目标 | 物理轮位 | 状态 |
|---|---|---|
| front/M1 | 未知 | 待视觉确认 |
| front/M2 | 未知 | 待视觉确认 |
| rear/M1 | 未知 | 待视觉确认 |
| rear/M2 | 未知 | 待视觉确认 |

## 4. 公司表中的寄存器与控制帧

### 4.1 `0x03` 读取

| 地址 | 公司表定义 |
|---:|---|
| `0x0000` | M1 命令转速/状态字 |
| `0x0001` | M2 命令转速/状态字 |
| `0x0002～0x0003` | M1 霍尔累计值高/低 16 位 |
| `0x0004～0x0005` | M2 霍尔累计值高/低 16 位 |
| `0x0006` | M1 反馈转速 rpm |
| `0x0007` | M2 反馈转速 rpm |
| `0x0008` | 模式：0 锁定、1 FOLLOW、2 自由 |
| `0x0009` | 母线电压，单位 100 mV |
| `0x000A` | 报警码 |
| `0x000B` | 程序版本 |

命令状态字高 12 位为 rpm，低 4 位状态为：0 自由停车、1 正转、2 反转、
3 刹车停车、4 减速到零。

### 4.2 `0x06` 单寄存器写

| 初始化顺序 | 操作 | 地址和值 |
|---:|---|---|
| 1 | 清报警 | `0x0009=0x0001` |
| 2 | 清霍尔 | `0x0007=0x0001` |
| 3 | 设置 FOLLOW | `0x000F=0x0001` |
| 4 | M1 减速时间置 0 | `0x0005=0x0000` |
| 5 | M2 减速时间置 0 | `0x0006=0x0000` |

减速时间单位为 100 ms，范围 `0～2000`。读取和写入地址不能互相推导：
`0x0008` 是模式读取地址，`0x000F` 是模式设置地址。

现有固件通信路径发送 `0x06` 后不读取或校验标准回显，因此当前工具也不把
严格回显作为写入成功条件，并在初始化结束后读取模式复核。此处描述实现行为，
不是采用《第四版.doc》后部示例。

### 4.3 自定义 `0x10` 双电机控制

该帧固定为 8 字节，不是标准 Modbus 写多个寄存器 PDU：

```text
01 10 M1_H M1_L M2_H M2_L CRC_LO CRC_HI

motor_word = (rpm << 4) | (state & 0x0F)
state: 0=自由停车, 1=正转, 2=反转, 3=刹车停车, 4=减速到零
```

当前实现不读取 `0x10` 响应。工具输出 `write=OK` 只证明 Linux tty 接收了
完整发送数据，不等于驱动板已经执行。

## 5. 当前小车工具及能力边界

工具：

```text
ros2 run sand_rake_hw_tools small_car_four_wheel_jog_once
```

已有约束：

- 只允许 `front/rear` 和 M1/M2；
- 前板固定 `/dev/ttyS6`，后板固定 `/dev/ttyS1`；
- 默认 dry-run；
- 真机执行要求显式确认架空、硬件停机手段和 tty 独占；
- 点动只允许 30/50/60/100 rpm，固定 300 ms；其中 50 rpm 控制字来自公司
  前部码表的明确编码；
- 动作前、动作后和信号中断时发送双电机 Coast；
- 点动时另一槽位保持 Coast；
- 动作前读取 `0x0008` 并要求 FOLLOW；
- 点动后 50 ms 从 `0x0000` 起读取全部 12 个状态寄存器，以同时捕获命令状态、
  霍尔、反馈转速、模式、母线电压、报警和版本；
- `--status-only` 读取 `0x0000～0x000B` 全部 12 个状态寄存器。

只读状态命令模板：

```bash
ros2 run sand_rake_hw_tools small_car_four_wheel_jog_once \
  --board front --status-only --device /dev/ttyS6 --execute \
  --confirm-wheels-off-ground \
  --confirm-hardware-stop-ready \
  --confirm-exclusive-tty
```

专用初始化入口为 `--initialize`。它在只读确认 FOLLOW 后先发送双 Coast，再按
第 4.2 节顺序发送五条 `0x06`，最后发送双 Coast，并在短暂静默和清空延迟字节
后重新读取 `0x0008` 复核 FOLLOW。工具不要求标准 `0x06` 严格回显；发送失败或
信号中断时会停止后续初始化步骤并尝试最终 Coast。

## 6. 已有实车证据

1. 前、后板收到双 Coast 帧 `01 10 00 00 00 00 C0 09` 后，原先锁死的
   四轮可以用手自由转动，证明 state 0 的机械效果为 Coast。
2. 30、60、100 rpm 的受控点动均未观察到轮胎转动。
3. front/M1 发送 100 rpm/state 1，即 `01 10 06 41 00 00 90 95`，50 ms 后
   从 `0x0000` 起读取两个状态字得到：

   ```text
   state_slot_0_raw = 0x0000
   state_slot_1_raw = 0x0641
   ```

   `0x0641=(100<<4)|1`。这是历史上“向 M1 发送、M2 状态字出现命令”的异常
   现象，不能用来推翻公司表规定的寄存器顺序，后续应按异常回读继续诊断。
4. 所有动作测试结束时均发送最终 Coast；用户随后确认四轮可自由转动。
5. 2026-08-16 车机重启后，两板状态字一度均为 `0x0003`（Brake）。
6. 用户明确授权并持续确认车辆架空后，前、后板均完成五步初始化，随后读取
   `0x0008=0x0001`，两状态字均为 `0x0000`（Coast）。
7. 初始化后执行 front/M1、30 rpm/state 1、300 ms 点动，状态区曾读回
   `state_slot_1_raw=0x01E1`，但现场确认没有轮子转动。
8. front/M2、rear/M1 等低速点动也未产生机械动作；后板一次动作期状态读取
   超时，但最终 Coast 成功，随后只读状态恢复正常。
9. 用户现场确认：急停已完全释放；没有独立驱动使能/自动开关；两块驱动板
   均亮绿灯；没有听到接触器或继电器动作声。Coast 后四轮均可用手自由转动。
10. 车机旧版源码已更新为当前本地版本。同步前备份为：

    ```text
    /home/rockchip/sand_rake_ws/src.backup-before-v4-sync-20260817
    /home/rockchip/sand_rake_ws/artifacts.backup-before-v4-sync-20260817
    ```

    备份源码目录含 `COLCON_IGNORE`，避免 colcon 发现重复包。车机工作区不是
    Git 仓库。
11. 更新后两板只读状态：模式 FOLLOW、报警 0、程序版本均为 `0x0004`；前板
    27.0 V，后板 26.1 V，静止时两路命令状态、霍尔及反馈转速均为 0。
12. 最新 front/M1、50 rpm/state 1、300 ms 测试帧为：

    ```text
    01 10 03 21 00 00 90 47
    ```

    速度帧写入约 50 ms 后开始读取完整 12 寄存器（读取耗时约 5.6 ms）：M1/M2
    状态均为 `0x0000`，两路霍尔和反馈转速均为 0，模式 `0x0001`，母线
    27.0 V，报警 0，版本 `0x0004`。最终 Coast 成功。车机检查不到占用
    `/dev/ttyS6`、`/dev/ttyS1` 的进程，也没有其他 ROS 节点；可排除已发现的
    后台程序立即覆盖命令。

## 7. 当前判断与下一步

当前“不转”不能再归因于车机仍运行旧程序，也没有证据支持串口占用、机械锁死、
母线欠压或板载报警：

- Coast 已产生可观察机械效果；
- 历史测试中 30/100 rpm 控制字曾异常出现在另一状态槽；最新 50 rpm 测试中，
  写入约 50 ms 后开始读取，两状态槽已经全部回到 0；
- 面板和 `0x0008` 均证明驱动板处于 FOLLOW。
- Coast 能解除四轮锁死，只证明全零控制帧可产生机械效果，不能证明非零速度
  字段被当前固件正确解释。

初始化入口、PTY 覆盖和双板实车初始化均已完成。两块不同位置的板表现一致，
当前优先怀疑共同的硬件输出使能条件，或板载程序版本 `0x0004` 与公司码表所
对应协议不一致；这只是基于现有证据的诊断方向，尚未得到厂家确认。下一步：

1. 先向公司确认驱动板程序版本 `0x0004` 是否与《第四版.doc》前部码表匹配，
   并确认是否存在码表未写明的硬件使能线、使能电平或额外使能命令。
2. 若厂家确认协议无误，再由具备电气测量能力的人员，在保持架空和可断电的
   条件下，测量 300 ms 速度命令期间驱动板电机输出端是否出现电压。不要由
   软件人员盲接表笔，也不要先提高转速或延长命令时间。
3. 输出端始终为 0：继续查使能、固件和驱动输出级；有输出但电机不动：再查
   电机供电回路、接线和电机本体。
4. 在公司答复或合格的电气测量结果出现前，不重复相同点动、不冻结轮位映射。

不得直接把五条 `0x06` 裸帧通过 `printf`、`echo -e` 或临时脚本发送到 tty。

## 8. 安全边界

- 真机动作时车辆必须持续架空，轮旁无人、无线缆或工具。
- 操作者必须在现场观察，并能立即硬件断电。
- 一次只测一块板的一个槽位，另一槽位始终 Coast。
- 串口、模式、帧写入或状态读取出现异常时立即停止并尝试最终 Coast。
- 在视觉确认前，不把候选轮位或方向写入正式 YAML。
- 不因无动作擅自超过 100 rpm 或延长 300 ms。
- 用户不在现场时，不执行真机写操作。

## 9. 软件参数

当前小车实测尺寸与软件参数已经统一：

| 参数 | 值 |
|---|---:|
| 带外皮轮子直径 | `0.26933 m` |
| 轮子半径 | `0.134665 m` |
| 实测轮距 | `0.53907 m` |
| 轴距 | `0.858 m`（`0.429 m × 2`） |
| 减速比 | `7.5` |
| 电机最低非零命令 | `30 rpm` |
| 电机最高命令 | `1500 rpm` |
| 控制频率 | `50 Hz` |

运动学已按参考代码执行整数 rpm 量化、非零命令最低 30 rpm 和最高 1500 rpm
限幅。当前以实测轮距作为有效轮距初值，后续可通过落地转向标定修正；轴距
不参与当前差速运动学计算。`0.15 m/s`、`0.80 rad/s` 等只是调试安全上限，
不是硬件规格。

## 10. 最终软件验证状态

2026-08-17 按公司表校正后，本地与车机均完成构建和测试。车机同步后曾进行
完整干净串行构建，结果为：

```text
依次构建：sand_rake_interfaces、sand_rake_control、sand_rake_hw_tools
测试总数：67
错误：0
失败：0
跳过：1
```

加入 50 rpm 和动作期完整状态读取后，本地重新构建/测试
`sand_rake_hw_tools` 成功（1 个 CTest 入口，0 错误/失败），车机也串行重编并
通过同一包测试。当前工程没有正式真车命令管线。下一对话应从第 7 节的厂家
确认/电气输出诊断继续，不要重复五步初始化，也不要引入未经现场验证的板级
四轮映射。
