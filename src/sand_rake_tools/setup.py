from setuptools import find_packages, setup

package_name = 'sand_rake_tools'

setup(
    name=package_name,
    version='0.0.0',
    packages=find_packages(exclude=['test']),
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='qzc',
    maintainer_email='15290799790@163.com',
    description=(
        'Shared recording, diagnostics, topic inspection, and data processing '
        'tools for the sand rake vehicle.'
    ),
    license='Apache-2.0',
    extras_require={
        'test': [
            'pytest',
        ],
    },
    entry_points={
        'console_scripts': [
        ],
    },
)
